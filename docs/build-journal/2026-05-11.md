# 2026-05-11 — Project kickoff

Scoped the reference build with Claude Code. Key decisions:

- **Phase 1 only.** Drone tender as the worked example. The predicted-vs-actual validation grid against `samples/Drone/` (Selection Board Report Final, Assessment Notes Final) is the credibility instrument — the one thing defended hardest if scope slips.
- **Audience: Declan Sheehan (NTA CTO).** Single 30-45 min demo. Target: first week of June 2026 — coincides with the hackathon window intentionally.
- **Stack divergence from the hackathon is intentional.** Hackathon stays Microsoft (Azure OpenAI, Semantic Kernel, Document Intelligence, Cosmos, Azure Container Apps). This reference build uses Claude API, local PDF/DOCX parsing, SQLite, Streamlit, `pandoc` + `prince` for PDF. Two builds, two stacks, one problem.
- **Open-source IP.** Repo `nta-pmi-agent`, Apache 2.0. Samples and drafts stay local; `.githooks/pre-commit` blocks accidental commits.
- **Build narrative is a deliverable.** This journal, the prompt log, commit hygiene, and 2-3 exhibit diffs are part of what Declan sees.

Cathal Bellew walkthrough this afternoon. Five decisions to land: repo name, LICENSE, whether to brief Mark Bradwell, Declan demo date, any cut-line reshuffles.

Repo stubbed: README, LICENSE, `.gitignore`, pre-commit guard, this journal, prompt-log convention, `.env.example`. No code yet — schemas (`RequirementsProfile`, `PreMarketReport`) come first when the build begins.

Scope doc (confidential, local-only): `~/Documents/NTA/tenders/roadmap/pmi-agent-claude-code-demo-scope.md`.

## Session 2 — Schemas and scaffolding

Compressed the original 4-week plan into 4 days against an end-of-week Declan demo. Cuts:

- Market Scanner dropped entirely (can return in a later session if there's time)
- Historical Analyst will read a hand-curated TED seed instead of live TED API — honest framing in the demo, the file is readable on screen
- Mullingar dropped; drone tender only
- Validation grid narrows to 3-5 predictions vs actuals for the drone tender

Project scaffolded with `uv`. Schemas locked: `RequirementsProfile`, `ResponseVolumeForecast`, `MarketDepth`, `CostSanityCheck`, `ScopeFlag`, `Recommendation` (closed taxonomy of six kinds), `PreMarketReport`. Provenance and source citations are mandatory at the type level.

3 smoke tests pass. Ruff clean. See `docs/prompt-log/2026-05-11-001-schemas-and-scaffolding.md` for the leading prompt and rationale.

Next: Spec Analyser v1 against the drone RFT.

## Session 3 — Spec Analyser end-to-end on the drone RFT

Built docx loader, SQLite audit log, Anthropic SDK wrapper (forced tool use, audit-logged), the Spec Analyser agent itself, and a `pmi-spec-analyse` CLI. 7 tests pass, ruff clean.

First live API call against `samples/Drone/RFT - Provision of Drone Timelapse and Video Production Services 30.12.2024.docx`:

- Model: `claude-opus-4-7`
- 27.5 s, 26,762 input tokens, 2,862 output tokens, ~$0.62
- Audit record `be1e9e82-...` written to `audit.db`

The extracted profile is solid — 7 service categories, 7 qualification barriers (including the "4 examples × 4 media formats" elimination rule), 10 unusual requirements (Russian sanctions, ISL accreditation, simultaneous nationwide filming, 3-min cap), 3 evaluation criteria with weights, full provenance. Hallucination guards held: `cpv_codes`/`nace_codes` empty (not in document), `value_band` bounds null with a `basis` explaining the rate-card structure.

This is the foundation for the Recommendation Agent — bundling risk and qualification overreach jump off the page.

See `docs/prompt-log/2026-05-11-002-spec-analyser.md` for the full session record.

Next: hand-curate the TED seed and build the Historical Analyst.

## Session 4 — Historical Analyst against a curated seed

Wrote a 6-record synthetic seed (`historical_seed.json`) spanning the range of media-production scopes — explicitly labelled in `_meta` as a placeholder for the live TED query. Built the seed loader, the agent, the `pmi-historical-analyse` CLI, and tests. 10 tests pass, ruff clean.

First live run against `reports/drone-spec.json`:
- 20.8 s, 7,529 in / 2,282 out tokens, ~$0.28
- Audit record `18bd58e9-…`
- **Predicted: 12-30 submissions, high confidence** — drone tender's actual was **28**, lands at the upper end. The agent specifically used the drone-specialist comparable as a lower-bound anchor because the all-four-formats rule narrows the pool.

Hit a Python 3.13 / hatchling editable-install incompatibility mid-session (3.13's site module skips `.pth` files with leading underscore; hatchling writes `_editable_impl_*.pth`). Pinned to Python 3.12 via `.python-version` and `requires-python = ">=3.11,<3.13"`. First concrete "Claude Code diagnosed and fixed an environment issue under time pressure" exhibit for the build narrative.

See `docs/prompt-log/2026-05-11-003-historical-analyst.md`.

Next: Recommendation Agent over closed taxonomy + orchestrator wiring the three agents into a single PreMarketReport pipeline.
